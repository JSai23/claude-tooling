# Python Environment

NEVER use `pip install` or `pip3 install`. Always use `uv` for Python package management.

- Install packages: `uv pip install {package}`
- Add to project: `uv add {package}`
- Run scripts: `uv run {script}`
- Create venvs: `uv venv`
